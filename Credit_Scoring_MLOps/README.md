# 📌 Промышленное развертывание системы кредитного скоринга (MLOps)

## 🧾 Описание проекта

Этот проект реализует **полный MLOps-цикл для кредитного скоринга**: от обучения и оптимизации модели до контейнеризации, мониторинга, A/B-экспериментов между версиями моделей и автоматизированного переобучения с помощью Airflow. Проект вдохновлен продакшен-системами, подчеркивая масштабируемость, надежность и автоматизацию.

## 🔧 Технологии

| Компонент          | Инструмент/Технология                |
|--------------------|--------------------------------------|
| API                | FastAPI + ONNX Runtime               |
| Модель             | Нейронная сеть (MLP)                 |
| Оптимизация        | ONNX + Квантование (INT8)            |
| Кеширование        | Redis                                |
| Мониторинг         | Prometheus + Grafana                 |
| Логи               | Loki + Promtail                      |
| Переобучение       | Airflow (DAGs)                       |
| Контейнеризация    | Docker + Docker Compose              |
| Продакшн           | Terraform + Kubernetes (артефакты)   |
| CI/CD              | GitHub Actions                       |
| Качество кода      | Pylint (через .idea конфиги)         |

---

## 📂 Структура проекта

```
Credit_Scoring_MLOps/
├── .github/workflows/          # CI/CD конфигурации (GitHub Actions)
├── data/                       # Данные для обучения и тестирования
├── dockerfiles/                # Dockerfiles для сборки образов
├── infrastructure/terraform/   # IaC с Terraform (не для локального запуска)
│   └── modules/                # Модули Terraform (kubernetes, monitoring, network, storage)
├── kubernetes/                 # Kubernetes манифесты для продакшена
├── ml_pipelines/               # ML-пайплайны (inference, optimizations, training)
├── models/                     # Предобученные модели и артефакты
├── monitoring/                 # Мониторинг, алерты и дашборды (Prometheus, Grafana, Evidently)
├── reports/                    # Отчеты и бенчмарки
├── src/                        # Исходный код
│   ├── api/                    # FastAPI-приложение для API
│   ├── dags/                   # Airflow DAG для переобучения
│   └── scripts/                # Дополнительные скрипты (бенчмарк, конвертация, оптимизация)
├── .gitignore                  # Игнорируемые файлы Git
├── docker-compose.yml          # Локальная настройка с Docker Compose
├── README.md                   # Документация проекта
├── requirements.txt            # Python-зависимости
└── run_scripts.bat             # Автоматизированный бат-файл для Windows (обучение, конвертация, оптимизация, бенчмаркинг)
```

---

## 🚀 Запуск всей системы локально

### 📌 Требования
- Установлены Docker + Docker Compose.
- Свободные порты: `8000`, `9090`, `3000`, `3100`, `6379`.
- Опционально: Python 3.8+ для ручных скриптов.

### ▶️ Сборка и старт
```bash
docker compose build  # Сборка всех образов
docker compose up -d  # Запуск сервисов в фоне
```

### 👀 Проверка
```bash
docker ps  # Проверьте запущенные контейнеры: credit-scoring-api, redis, prometheus, grafana, loki, promtail
```

---

## 🌐 API: Инференс и эксперименты

### 📍 Проверка здоровья сервиса
```bash
curl http://localhost:8000/health
# {"status": "ok"}
```

### 🧠 Стандартное предсказание
**Endpoint:** `POST /predict`  
**Пример запроса:**
```json
{
  "batch": [
    {"values": [22, 94594, 26737, 1, 0.226, 0.121, 38]},
    {"values": [30, 70000, 150000, 2, 0.150, 0.050, 7]}
  ]
}
```
- `values`: фичи (например, возраст, доход, сумма кредита и т.д.).

### ⚡ Предсказание с кешированием (Redis)
**Endpoint:** `POST /predict_cached`  
Повторные запросы вернут `"cached": true` для быстрого ответа.  
**Пример ответа:**
```json
{
  "predictions": [0.143],
  "cached": true
}
```

### 🧪 A/B-эксперимент между моделями
**Endpoint:** `POST /predict_ab`  
Варианты:
- `"variant": "A"` → Стандартная ONNX-модель
- `"variant": "B"` → Квантованная INT8-модель  
**Пример ответа:**
```json
{
  "variant": "A",
  "predictions": [0.91]
}
```

### 🔑 Документация API
- Интерактивный Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

---

## 📊 Мониторинг и логи

### 📈 Prometheus (метрики)
- Доступ: http://localhost:9090 (веб-интерфейс)
- Метрики API: http://localhost:8000/metrics

### 📊 Grafana (дашборды + Loki логи)
- Доступ: http://localhost:3000 (логин: `admin` / `admin`)
- Преднастроенный дашборд: "Credit API Overview" (из `credit-api-overview.json`)
- Интегрированы: Prometheus и Loki.

### 🪵 Просмотр логов API через Loki
- Grafana → Explore
- Выберите источник Loki
- Запрос: `{container="credit-scoring-api"}`
- Помогает отлаживать ошибки API (например, `ModuleNotFoundError: No module named 'api'`).

---

## 🧠 Ручные workflows с моделями

### 🪟 Автоматизация на Windows (run_scripts.bat)

Для пользователей Windows предусмотрена автоматизация всех шагов обучения, конвертации, оптимизации и бенчмаркинга через `run_scripts.bat`:

```cmd
run_scripts.bat
```

Этот бат-файл последовательно выполняет:
1. **Обучение модели**: Загружает данные, обучает MLP-модель, сохраняет артефакты и отчеты.
2. **Конвертацию в ONNX**: Преобразует sklearn-модель в ONNX-формат.
3. **Оптимизацию (квантование)**: Применяет INT8-квантование для ускорения инференса.
4. **Бенчмаркинг**: Сравнивает производительность sklearn, ONNX и квантованного ONNX.

Вывод каждого шага отображается в консоли с паузами для контроля. Убедитесь, что виртуальное окружение активировано и все зависимости установлены. Файл переключает консоль на UTF-8 для корректного отображения русских сообщений.

### ✔️ Обучение модели
```bash
cd ml_pipelines/training
python train_model.py --data ../../data/train.csv --output ../../models/model_sklearn.pkl --report ../../reports/
```
- Вывод: Модель в `models/`, отчет классификации в `reports/`.

### 🔄 Конвертация в ONNX
```bash
cd ml_pipelines/optimizations
python onnx_conversion.py --data ../../data/train.csv --model ../../models/model_sklearn.pkl --onnx ../../models/default_model.onnx
```
- Альтернатива: `src/scripts/onnx_conversion.py`.

### 📌 Квантование ONNX (оптимизация)
```bash
python optimize_onnx.py --model ../../models/default_model.onnx --output ../../models/default_model_int.onnx
```
- Создает INT8-версию для эффективности.
- Альтернатива: `src/scripts/optimize_onnx.py`.

### 🚀 Бенчмарк инференса
```bash
cd src/scripts
python benchmark_inference.py
```
- Сравнивает производительность sklearn, ONNX и квантованного ONNX (вывод в консоль/логи).

---

## 🚀 Автоматизированное переобучение (Airflow)

**DAG:** `src/dags/retraining_dag.py`  
- Запускает обучение, конвертацию и обновления деплоя.  
- Для локального тестирования: Установите Airflow (`pip install apache-airflow`), затем `airflow dags trigger retraining_dag`.  
- **Примечание:** Airflow не в `docker-compose.yml`; добавьте как сервис для оркестрации.

---

## ☁️ Продакшн-артефакты

- **Terraform (infrastructure/terraform/):** Развертывает облачные ресурсы (VPC, Kubernetes, хранилище) — запуск через `terraform apply` в облачной среде.
- **Kubernetes (kubernetes/):** Деплой API с HPA, canary-релизом (90/10), ingress, секретами. Примените через `kubectl apply -f kubernetes/`.
- **CI/CD (.github/workflows/ci-cd.yml):** Автоматизирует сборки, тесты и деплои на пуши/PR.

**Не запускайте локально** — это артефакты для продакшена (например, AWS/GCP).

---

## 🏁 Чек-лист проверки

| Проверка              | Команда/Метод                          |
|-----------------------|----------------------------------------|
| API работает          | `curl localhost:8000/health`           |
| Swagger UI            | http://localhost:8000/docs             |
| Кеш работает          | Повторные вызовы `/predict_cached`     |
| A/B переключается     | Множественные `/predict_ab`            |
| Метрики собираются    | http://localhost:8000/metrics          |
| Prometheus скрейпит   | http://localhost:9090                  |
| Grafana дашборд       | http://localhost:3000 (admin/admin)    |
| Логи API              | Loki в Grafana: `{container="credit-scoring-api"}` |
| Bat-файл выполняется (Windows) | Запуск `run_scripts.bat` и проверка вывода |
| Airflow DAG (если запущен) | Проверьте статус в Airflow UI      |

---